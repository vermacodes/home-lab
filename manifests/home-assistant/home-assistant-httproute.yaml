# HTTPRoute for Home Assistant web interface
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: home-assistant-https-route
  namespace: home-assistant
spec:
  hostnames:
  - ha.ashishverma.dev
  parentRefs:
  - name: envoy
    namespace: envoy-gateway-system
    sectionName: https
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: home-assistant
      port: 8080
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /
